/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        unoptimized: true,
        domains: ['assets.aceternity.com', "cloud.appwrite.io"]
    },
};

export default nextConfig;
