/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['assets.aceternity.com', "cloud.appwrite.io"],
    unoptimized: true,
    },
};

export default nextConfig;
